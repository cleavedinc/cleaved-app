// import { graphql } from "../../../generated-types";

// export const PROJECT_SET_PROGRESS = graphql(`
//   mutation projectSetProgress($projectId: ID!, $organizationId: ID!, $progress: Int!) {
//     projectSetProgress(projectId: $projectId, organizationId: $organizationId, progress: $progress)
//   }
// `);
