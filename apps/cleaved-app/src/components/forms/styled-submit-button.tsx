import styled from "styled-components";

import { ButtonPrimary, FONT_SIZES, SPACING_PX } from "@cleaved/ui";

export const StyledSubmitButton = styled(ButtonPrimary)`
  font-size: ${FONT_SIZES.MEDIUM};
  margin-left: auto;
  margin-top: ${SPACING_PX.ONE};
`;
