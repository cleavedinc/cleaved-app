import { SPACING } from "@cleaved/ui";
import styled from "styled-components";

export const StyledHelperInfoTextWrapper = styled.div`
  align-self: flex-start;
  margin-bottom: ${SPACING.MEDIUM};
`;
