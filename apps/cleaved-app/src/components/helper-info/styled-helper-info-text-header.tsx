import styled from "styled-components";

import { FONT_WEIGHTS, SectionHeader } from "@cleaved/ui";

export const StyledHelperInfoTextHeader = styled(SectionHeader)`
  font-weight: ${FONT_WEIGHTS.MEDIUM_LIGHT};
`;
