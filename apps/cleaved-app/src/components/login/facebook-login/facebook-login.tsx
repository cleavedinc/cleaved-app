// import React, { FunctionComponent } from "react";
// import FacebookLogin, { ReactFacebookLoginProps } from "react-facebook-login";

// export type FacebookLoginWrapperProps = ReactFacebookLoginProps;

// export const FacebookLoginWrapper: FunctionComponent<FacebookLoginWrapperProps> = (props) => {
//   const { autoLoad, appId, callback, fields } = props;

//   return <FacebookLogin appId={appId} autoLoad={autoLoad} fields={fields} callback={callback} />;
// };
