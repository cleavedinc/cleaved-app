import styled from "styled-components";

import { SPACING } from "@cleaved/ui";

import { HelperInfoHeaderTextImageRightBox } from "../../../components";

export const StyledHelperInfoHeaderTextImageRightBox = styled(HelperInfoHeaderTextImageRightBox)`
  margin-bottom: 0;
  margin-top: ${SPACING.MEDIUM};
  padding-left: 0;
  padding-right: 0;
`;
