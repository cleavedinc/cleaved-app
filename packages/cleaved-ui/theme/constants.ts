import { unitFormat } from "./utils";

export const LEFT_SIDEBAR_WIDTH: string = unitFormat(320);

export const TEXTAREA_MAX_HEIGHT: string = unitFormat(100);
export const TEXTAREA_MIN_HEIGHT: string = unitFormat(100);
