import styled from "styled-components";
import { SPACING_PX } from "../../theme";

export const OrderedListLowerAlpha = styled.ol`
  list-style-type: lower-alpha;
  margin-bottom: ${SPACING_PX.THREE};
  padding-left: 16px;
`;
